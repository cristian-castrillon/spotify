<div class="row">
  <% @artists.each do |artist| %>
    <div class="col-sm-6 col-md-3">
      <div class="thumbnail">
        <img src=<%= artist.image_url %> alt=<%= artist.name %>>
        <div class="caption">
          <h3 class="text-center"><%= artist.name %></h3>
          <p class="text-center"><%= link_to("Ver más", artist_path(artist.id), class: "btn btn-primary") %></p>
        </div>
      </div>
    </div>
  <% end %>
</div>